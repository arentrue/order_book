box:
  id: erlang:21
  volumes: $HOST_HOME/.cache:/root/.cache

dev:
  steps:
  - internal/shell

build:
  steps:
  - script:
    name: compile
    code: make compile

#   ToDo
# - script:
#   name: lint
#   code: make compile

  - script:
    name: xref
    code: make xref
  - script:
    name: dialyze
    code: make dialyze
  - script:
    name: test
    code: make test
  - script:
    name: release
    code: make release
